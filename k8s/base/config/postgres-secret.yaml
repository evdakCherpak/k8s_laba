apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
type: Opaque
stringData:
  POSTGRES_USER: messenger
  POSTGRES_PASSWORD: messenger123
  # bootstrap-БД; usersdb и messagesdb создаёт init-скрипт (postgres-initdb)
  POSTGRES_DB: messenger
